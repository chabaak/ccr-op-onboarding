{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "incidentCover.schema.json",
  "title": "datapack incidentCover.json — 요원 파일 사건 표지 문안",
  "description": "AGENT FILE 표지의 사건 개요 본문과 현장 요원 임무 문안. 표지는 선택된 사건의 시각, 장소, 날씨, 신고 문장을 담으므로 meta.json과 분리된 팩별 문안 파일로 관리한다.",
  "type": "object",
  "required": ["incident", "mission"],
  "additionalProperties": false,
  "properties": {
    "incident": {
      "type": "object",
      "required": ["body"],
      "additionalProperties": false,
      "properties": {
        "body": {
          "type": "array",
          "minItems": 3,
          "maxItems": 3,
          "items": { "type": "string", "minLength": 1 },
          "description": "사건 개요의 세 줄. 각 항목은 표지에서 별도 줄로 렌더된다."
        }
      }
    },
    "mission": {
      "type": "string",
      "minLength": 1,
      "description": "현장 요원 임무 섹션의 한 줄 문안."
    }
  }
}
