{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "incidentBrief.schema.json",
  "title": "datapack incidentBrief.json — 선택 전 사건 개요 문안",
  "description": "데스크톱 사건 파일을 열 때 확인판에 표시하는 팩별 사건 개요. 판의 제목, 버튼, 포털 공통 안내는 클라이언트가 소유하고, 이 파일은 사건마다 달라지는 사실 문안만 담는다.",
  "type": "object",
  "required": ["lead", "body"],
  "additionalProperties": false,
  "properties": {
    "lead": {
      "type": "string",
      "minLength": 1,
      "description": "확인판의 첫 문장. 사건 장소와 출발 상황처럼 이 팩만의 개요를 쓴다."
    },
    "body": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 },
      "description": "확인판 보조 문장. 다른 팩도 그대로 쓸 포털 공통 문장은 넣지 않는다."
    }
  }
}
